# Malicious packages notify

This script serves the simple purpose to forward the malicious packge index
updates in a JSON format to a [Telegram channel](https://t.me/mal_packages).
The script is executed once per hour.
